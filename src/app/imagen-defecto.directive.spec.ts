import { ImagenDefectoDirective } from './imagen-defecto.directive';

describe('ImagenDefectoDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenDefectoDirective();
    expect(directive).toBeTruthy();
  });
});
